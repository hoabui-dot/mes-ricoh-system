version: '3.8'

name: station-agent-production

services:
  # ─────────────────────────────────────────────
  # Infrastructure Services
  # ─────────────────────────────────────────────
  redis:
    image: redis:7.4-alpine
    container_name: production-station-redis
    restart: always
    command: redis-server --appendonly yes --requirepass "${REDIS_PASSWORD}"
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - station-net
      - platform-net
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Kafka is provided by the shared platform Kafka cluster on platform-net.
  # 1. Station Gateway Service (HTTP Gateway)
  # ─────────────────────────────────────────────
  station-gateway:
    image: ${DOCKER_REGISTRY:-vanhoadotbui2628}/station-gateway:${VERSION:-latest}
    container_name: production-station-gateway
    restart: always
    ports:
      - "${PORT_STATION_GATEWAY:-5001}:5001"
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - ASPNETCORE_HTTP_PORTS=5001
      - ASPNETCORE_ENVIRONMENT=Production
      - STATION_ID=${STATION_ID}
      - REDIS_CONNECTION_STRING=redis:6379,password=${REDIS_PASSWORD}
      - SQLITE_GATEWAY_PATH=${SQLITE_GATEWAY_PATH:-/data/gateway.db}
      - Gateway__OutboxBatchSize=10
      - Gateway__OutboxIntervalSeconds=5
      - Kafka__BootstrapServers=${STATION_KAFKA_BOOTSTRAP_SERVERS:-kafka:29092}
      - Kafka__EnableIdempotence=true
      - PRINTER_ADAPTER_URL=${PRINTER_ADAPTER_URL:?PRINTER_ADAPTER_URL is required}
      - Kafka__DefaultExchange=station.events
    volumes:
      - sqlite-data:/data
      - mqtt-logs:/logs
    networks:
      - station-net
      - platform-net
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ─────────────────────────────────────────────
  # 2. Job Engine Service
  # ─────────────────────────────────────────────
  job-engine:
    image: ${DOCKER_REGISTRY:-vanhoadotbui2628}/job-engine:${VERSION:-latest}
    container_name: production-station-job-engine
    restart: always
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "5002:5002"
    environment:
      - STATION_ID=${STATION_ID}
      - REDIS_CONNECTION_STRING=redis:6379,password=${REDIS_PASSWORD}
      - SQLITE_JOB_ENGINE_PATH=/data/job_engine.db
      - Kafka__BootstrapServers=${STATION_KAFKA_BOOTSTRAP_SERVERS:-kafka:29092}
      - Kafka__EnableIdempotence=true
      - Kafka__DefaultExchange=station.events
      - ASPNETCORE_ENVIRONMENT=Production
    volumes:
      - sqlite-data:/data
      - job-engine-logs:/logs
    networks:
      - station-net
      - platform-net
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5002/health"]
      interval: 15s
      timeout: 5s
      retries: 3

  # ─────────────────────────────────────────────
  # 4. Laser Adapter Service
  # ─────────────────────────────────────────────
  laser-adapter:
    image: ${DOCKER_REGISTRY:-vanhoadotbui2628}/laser-adapter:${VERSION:-latest}
    container_name: production-station-laser-adapter
    restart: always
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "5004:5004"
    environment:
      - STATION_ID=${STATION_ID}
      - REDIS_CONNECTION_STRING=redis:6379,password=${REDIS_PASSWORD}
      - SQLITE_LASER_PATH=/data/laser.db
      - Laser__Host=${PRODUCTION_LASER_IP}
      - Laser__Port=${PRODUCTION_LASER_PORT:-8901}
      - Kafka__BootstrapServers=${STATION_KAFKA_BOOTSTRAP_SERVERS:-kafka:29092}
      - Kafka__EnableIdempotence=true
      - Kafka__DefaultExchange=station.events
      - ASPNETCORE_ENVIRONMENT=Production
    volumes:
      - sqlite-data:/data
      - laser-logs:/logs
    networks:
      - station-net
      - platform-net
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ─────────────────────────────────────────────
  # 5. Vision Service
  # ─────────────────────────────────────────────
  vision-service:
    image: ${DOCKER_REGISTRY:-vanhoadotbui2628}/vision-service:${VERSION:-latest}
    container_name: production-station-vision-service
    restart: always
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "5005:5005"
    environment:
      - STATION_ID=${STATION_ID}
      - REDIS_CONNECTION_STRING=redis:6379,password=${REDIS_PASSWORD}
      - SQLITE_VISION_PATH=/data/vision.db
      - VISION_IMAGE_STORAGE_PATH=/storage/vision
      - VisionCamera__Host=${PRODUCTION_CAMERA_IP}
      - VisionCamera__Port=${PRODUCTION_CAMERA_PORT:-80}
      - Kafka__BootstrapServers=${STATION_KAFKA_BOOTSTRAP_SERVERS:-kafka:29092}
      - Kafka__EnableIdempotence=true
      - Kafka__DefaultExchange=station.events
      - ASPNETCORE_ENVIRONMENT=Production
    volumes:
      - sqlite-data:/data
      - vision-logs:/logs
      - vision-storage:/storage
    networks:
      - station-net
      - platform-net
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ─────────────────────────────────────────────
  # 6. PLC Adapter Service
  # ─────────────────────────────────────────────
  plc-adapter:
    image: ${DOCKER_REGISTRY:-vanhoadotbui2628}/plc-adapter:${VERSION:-latest}
    container_name: production-station-plc-adapter
    restart: always
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "5006:5006"
    environment:
      - STATION_ID=${STATION_ID}
      - REDIS_CONNECTION_STRING=redis:6379,password=${REDIS_PASSWORD}
      - SQLITE_PLC_PATH=/data/plc.db
      - Plc__Host=${PRODUCTION_PLC_IP}
      - Plc__Port=${PRODUCTION_PLC_PORT:-502}
      - Kafka__BootstrapServers=${STATION_KAFKA_BOOTSTRAP_SERVERS:-kafka:29092}
      - Kafka__EnableIdempotence=true
      - Kafka__DefaultExchange=station.events
      - ASPNETCORE_ENVIRONMENT=Production
    volumes:
      - sqlite-data:/data
      - plc-logs:/logs
    networks:
      - station-net
      - platform-net
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ─────────────────────────────────────────────
  # 7. Kiosk UI Service
  # ─────────────────────────────────────────────
  kiosk-ui:
    image: ${DOCKER_REGISTRY:-vanhoadotbui2628}/kiosk-ui:${VERSION:-latest}
    container_name: production-station-kiosk-ui
    restart: always
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "5222:5007"
    environment:
      - STATION_ID=${STATION_ID}
      - REDIS_CONNECTION_STRING=redis:6379,password=${REDIS_PASSWORD}
      - SQLITE_KIOSK_PATH=/data/kiosk.db
      - Jwt__Secret=${JWT_SECRET}
      - Jwt__Issuer=${JWT_ISSUER:-nd-station-agent}
      - Jwt__Audience=${JWT_AUDIENCE:-nd-kiosk}
      - Jwt__ExpiryMinutes=${JWT_EXPIRY_MINUTES:-480}
      - ASPNETCORE_ENVIRONMENT=Production
      - JOB_ENGINE_HOST=${JOB_ENGINE_HOST}
      - JOB_ENGINE_PORT=${JOB_ENGINE_PORT}
      - PRINTER_ADAPTER_URL=${PRINTER_ADAPTER_URL:?PRINTER_ADAPTER_URL is required}
      - CORS_ORIGINS=${CORS_ORIGINS}
    volumes:
      - sqlite-data:/data
      - kiosk-logs:/logs
    networks:
      - station-net
      - platform-net
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ─────────────────────────────────────────────
  # 8. Projection Service
  # ─────────────────────────────────────────────
  projection-service:
    image: ${DOCKER_REGISTRY:-vanhoadotbui2628}/projection-service:${VERSION:-latest}
    container_name: production-station-projection-service
    restart: always
    ports:
      - "5009:5009"
    environment:
      - STATION_ID=${STATION_ID}
      - SQLITE_PROJECTION_PATH=/data/projection.db
      - Kafka__BootstrapServers=${STATION_KAFKA_BOOTSTRAP_SERVERS:-kafka:29092}
      - Kafka__EnableIdempotence=true
      - Kafka__DefaultExchange=station.events
      - ASPNETCORE_ENVIRONMENT=Production
      - PRINTER_ADAPTER_URL=${PRINTER_ADAPTER_URL:?PRINTER_ADAPTER_URL is required}
    volumes:
      - sqlite-data:/data
      - projection-logs:/logs
    networks:
      - station-net
      - platform-net
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5009/health"]
      interval: 15s
      timeout: 5s
      retries: 3

# ─────────────────────────────────────────────
# Volumes Configuration (Production Mounts)
# ─────────────────────────────────────────────
volumes:
  redis-data:
  sqlite-data:
  mqtt-logs:
  job-engine-logs:
  laser-logs:
  vision-logs:
  vision-storage:
  plc-logs:
  kiosk-logs:
  projection-logs:

# ─────────────────────────────────────────────
# Networks Configuration
# ─────────────────────────────────────────────
networks:
  station-net:
    driver: bridge
    name: station-net-production
  platform-net:
    external: true
    name: platform-net
