import { NavLink } from 'react-router-dom';
import { ClipboardCheck, FileWarning, Gauge, ListChecks, ShieldCheck, Tags } from 'lucide-react';
import { useI18n } from '@mom-platform/i18n-ui-shared';
const links = [{ to: '/dashboard', key: 'nav.dashboard', icon: Gauge }, { to: '/inspection/results', key: 'nav.results', icon: ClipboardCheck }, { to: '/inspection/plans', key: 'nav.plans', icon: ListChecks }, { to: '/inspection/defect-codes', key: 'nav.defects', icon: Tags }, { to: '/nonconformance/ncr', key: 'nav.ncr', icon: FileWarning }, { to: '/nonconformance/capa', key: 'nav.capa', icon: ShieldCheck }];
export function Sidebar() { const { t } = useI18n(); return <aside className="hidden w-64 shrink-0 border-r border-border bg-[var(--navy-950)] text-slate-100 lg:block"><div className="border-b border-white/10 px-5 py-5"><div className="text-lg font-black tracking-tight">{t('app.name')}</div><div className="mt-1 text-xs text-slate-400">{t('app.subtitle')}</div></div><nav className="space-y-1 p-3">{links.map(({ to, key, icon: Icon }) => <NavLink key={to} to={to} className={({ isActive }) => `flex items-center gap-3 rounded-md px-3 py-2.5 text-sm font-semibold transition ${isActive ? 'bg-action text-action-foreground' : 'text-slate-300 hover:bg-white/10 hover:text-white'}`}><Icon className="h-4 w-4" />{t(key)}</NavLink>)}</nav></aside>; }
