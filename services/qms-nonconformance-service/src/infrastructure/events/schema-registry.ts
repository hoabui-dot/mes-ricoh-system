const EVENTS = ['QMS.Nonconformance.NCRRaised.v1', 'QMS.Nonconformance.NCRDispositioned.v1', 'QMS.Nonconformance.CAPAClosed.v1'];
export async function registerEventSchemas(url: string): Promise<void> { for (const eventType of EVENTS) { const schema = { type: 'object', additionalProperties: true, properties: { event_id: { type: 'string' }, event_type: { type: 'string' }, occurred_at: { type: 'string' }, source_service: { type: 'string' }, trace_id: { type: 'string' }, payload: { type: 'object', additionalProperties: true } }, required: ['event_id','event_type','occurred_at','source_service','trace_id','payload'] }; const response = await fetch(`${url.replace(/\/$/, '')}/subjects/${eventType}-value/versions`, { method: 'POST', headers: { 'content-type': 'application/vnd.schemaregistry.v1+json' }, body: JSON.stringify({ schemaType: 'JSON', schema: JSON.stringify(schema) }) }); if (!response.ok && response.status !== 409) throw new Error(`Schema Registry registration failed for ${eventType}: ${response.status}`); } }
