import React, { useEffect, useState } from 'react';
import { ArrowLeft, Plus, Save } from 'lucide-react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { useAuth } from '../../context/AuthContext';
import { useI18n } from '@mom-platform/i18n-ui-shared';
import { fetchResource, postResource } from '../../lib/masterDataApi';
import { Button, SelectBase } from '../../components/ui';
import { toast } from 'sonner';

function text(value: unknown): string { if (typeof value === 'string') return value; if (!value || typeof value !== 'object') return ''; const item = value as Record<string, unknown>; return String(item.vi || item.en || item.ja || item.ko || ''); }

export const RoutingOperationsScreen: React.FC = () => {
  const { id } = useParams(); const { user } = useAuth(); const { t } = useI18n(); const navigate = useNavigate();
  const [routing, setRouting] = useState<any>(null); const [operations, setOperations] = useState<any[]>([]); const [workCenters, setWorkCenters] = useState<any[]>([]); const [rows, setRows] = useState<any[]>([]); const [saving, setSaving] = useState(false);
  const [form, setForm] = useState({ operation_id: '', work_center_id: '', seq: 10, predecessor_seq: '' });
  const load = async () => { if (!id) return; try { const [headers, operationRows, wcRows, routeRows] = await Promise.all([fetchResource('routing-headers', user), fetchResource('operations', user), fetchResource('work-centers', user), fetchResource('routing-operations', user, '?limit=500')]); setRouting(headers.find((row: any) => row.master_id === id)); setOperations(operationRows); setWorkCenters(wcRows); setRows(routeRows.filter((row: any) => row.routing_header_id === id).sort((a: any, b: any) => Number(a.seq) - Number(b.seq))); setForm((current) => ({ ...current, operation_id: current.operation_id || operationRows[0]?.master_id || '', work_center_id: current.work_center_id || wcRows[0]?.master_id || '' })); } catch (error: any) { toast.error(error.message); } };
  useEffect(() => { void load(); }, [id, user?.userId]);
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); try { const operation = operations.find((row) => row.master_id === form.operation_id); await postResource('routing-operations', { code: `${routing?.code || 'ROUTING'}-${String(form.seq).padStart(3, '0')}`, name: text(operation?.name) || operation?.code || 'Operation', routing_header_id: id, operation_id: form.operation_id, work_center_id: form.work_center_id, seq: Number(form.seq), predecessor_seq: form.predecessor_seq ? Number(form.predecessor_seq) : null }, user); toast.success(t('routing.operationAdded')); await load(); } catch (error: any) { toast.error(error.message); } finally { setSaving(false); } };
  return <div className="mx-auto max-w-6xl space-y-6"><div className="flex items-center justify-between"><Link to="/master-data/routings" className="inline-flex items-center gap-2 text-sm text-sky-300"><ArrowLeft className="h-4 w-4" />{t('routing.title')}</Link></div><div className="mes-page-header"><div><h1 className="text-xl font-bold text-slate-100">{routing?.code || t('routing.title')}</h1><p className="text-sm text-slate-300">{text(routing?.name)}</p><p className="text-xs text-slate-400">{text(routing?.description)}</p></div></div><div className="overflow-hidden rounded-md border border-slate-800 bg-slate-900"><table className="mes-table"><thead><tr><th>{t('mbom.seq')}</th><th>{t('routing.operation')}</th><th>{t('routing.workCenter')}</th><th>{t('routing.confirmation')}</th><th>{t('routing.scan')}</th><th>{t('routing.outputLabel')}</th></tr></thead><tbody>{rows.map((row, index) => <tr key={row.master_id}><td className="font-mono">{index + 1}</td><td><div className="font-semibold">{row.operation_code}</div><div className="text-xs text-slate-300">{text(row.operation_name)}</div><div className="text-xs text-slate-500">{text(row.operation_description)}</div></td><td><div>{row.work_center_code}</div><div className="text-xs text-slate-400">{text(row.work_center_name)}</div></td><td>{row.confirmation_mode}</td><td>{row.requires_material_scan ? t('common.yes') : t('common.no')}</td><td>{row.requires_output_label ? t('common.yes') : t('common.no')}</td></tr>)}</tbody></table>{rows.length === 0 && <div className="p-8 text-center text-slate-500">{t('routing.noOperations')}</div>}</div><form onSubmit={submit} className="grid gap-4 rounded-md border border-slate-800 bg-slate-900 p-6 sm:grid-cols-4"><label className="space-y-1"><span className="text-sm text-slate-300">{t('routing.operation')} *</span><SelectBase required value={form.operation_id} onValueChange={(value) => setForm({ ...form, operation_id: value })} options={operations.map((row) => ({ value: row.master_id, label: `${row.code} — ${text(row.name)}` }))} aria-label={t('routing.operation')} /></label><label className="space-y-1"><span className="text-sm text-slate-300">{t('routing.workCenter')} *</span><SelectBase required value={form.work_center_id} onValueChange={(value) => setForm({ ...form, work_center_id: value })} options={workCenters.map((row) => ({ value: row.master_id, label: `${row.code} — ${text(row.name)}` }))} aria-label={t('routing.workCenter')} /></label><label className="space-y-1"><span className="text-sm text-slate-300">{t('mbom.seq')} *</span><input required type="number" min="1" value={form.seq} onChange={(e) => setForm({ ...form, seq: Number(e.target.value) })} className="w-full rounded-md border border-slate-700 bg-slate-950 p-3 text-slate-100" /></label><label className="space-y-1"><span className="text-sm text-slate-300">{t('routing.predecessor')}</span><input type="number" min="1" value={form.predecessor_seq} onChange={(e) => setForm({ ...form, predecessor_seq: e.target.value })} className="w-full rounded-md border border-slate-700 bg-slate-950 p-3 text-slate-100" /></label><div className="sm:col-span-4 flex justify-end"><Button type="submit" disabled={saving}><Plus className="h-4 w-4" />{t('routing.addOperation')}</Button></div></form></div>;
};
