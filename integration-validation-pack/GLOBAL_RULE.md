# GLOBAL VALIDATION RULE

- Preserve current architecture.
- Never verify using fabricated data unless explicitly defined as a disposable fixture.
- Every change must rerun affected business flows.
- Runtime evidence is required for release.
- Stop immediately if validation requires changing shared ownership or event semantics.
- Record blockers instead of bypassing them.
